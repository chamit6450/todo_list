import React from 'react';

export default function Task({ data }) {
  return (
    <div>
      {/* <ul>
        {data.map((item, index) => (
            <>
          <li key={index}>{item}</li>
          <button>Remove</button>
            </>
          ))}
      </ul> */}
    </div>
  );
}
